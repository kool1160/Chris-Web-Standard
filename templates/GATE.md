# Gate `<ID>` — `<NAME>`

## Objective

`<One complete visitor/business outcome.>`

## Why now

`<Why this is the next constraint.>`

## In scope

- `<page/content/feature>`
- `<page/content/feature>`
- `<integration if required>`

## Out of scope

- `<future page/feature>`
- `<redesign not required here>`
- `<CMS/auth/database/tracking/app behavior not required here>`

## Acceptance criteria

- [ ] `<visitor behavior>`
- [ ] `<mobile/responsive behavior>`
- [ ] `<accessibility/performance/SEO/form behavior as applicable>`
- [ ] `<failure/fallback behavior>`
- [ ] Required quality checks pass.

## Required evidence

- exact PR/head SHA;
- exact build/test/audit commands and results;
- browser/device evidence at documented sizes when relevant;
- screenshots only when they materially prove visual state;
- unresolved blocking review threads: zero.

## Production / rollback

`<How this change can be previewed, deployed, and reversed safely.>`

## Stop conditions

Stop and escalate if the gate requires a business-scope change, brand redesign, production-domain/DNS change, new tracking/privacy posture, paid service/billing, customer/private data boundary, destructive content/data action, or material architecture expansion.